export type CircleType = {
  cx?: number | string;
  cy?: number | string;
  r?: number | string;
};

export type PathType = {
  d: string;
};

export type LineType = {
  x1: number | string;
  y1: number | string;
  x2: number | string;
  y2: number | string;
};
